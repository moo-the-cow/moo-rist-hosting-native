Check out the zip files for your Operating System

+ [Windows](windows/)

+ [Linux (x86_64)](linux/)

+ [Linux (arm64)](linux_arm64/)

+ [Mac (aarch64)](mac/)

+ [Linux (armv7l 32-bit)](linux_armv7l_32bit/) - raspberry pi2 v1.1

#### For all Platforms:
Create a folder `rist-selfhosting` Copy both Zip files into that folder and extract both so it looks like
#### Windows
```
rist-selfhosting/
├── librist.zip
├── StatsServer.zip
├── banner.txt
├── lofile.json
├── librist
|   └── tools
|       ├── ristreceiver.exe
|       └── ristsender.exe
└── StatsServer.exe
```

Download the bat file into your `rist-selfhosting` folder and double click the `RIST_Selfhosting.bat`

For the Remote Scenario use the `Rist_Selfhosting-remote.bat`

#### Linux, linux_arm64, macos
```
rist-selfhosting/
├── librist.zip
├── StatsServer.zip
├── banner.txt
├── lofile.json
├── librist
|   └── tools
|       ├── ristreceiver
|       └── ristsender
└── StatsServer
```

Download the shell script `Rist_Selfhosting.sh` into your `rist-selfhosting` folder and execute it

```
bash RIST_Selfhosting.sh
```
For the Remote Scenario use the `Rist_Selfhosting-remote.sh`

------------
so for both versions this will create a `credentials.txt` file that's getting autogenerated.

you can change the username or password any time, after it's generated the file will be used for authentication.

```
USERNAME=moo-msi9k7bifhjftdjr
PASSWORD=peqt78sdxei83x6kdh2c0zoquaq7i4
```
